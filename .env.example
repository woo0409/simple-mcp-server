# MCP Server Configuration
MCP_SERVER_NAME=simple-mcp-server
MCP_SERVER_PORT=8000
MCP_SERVER_HOST=0.0.0.0

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# MySQL Database Configuration
# For Docker: use host.docker.internal (Mac/Windows) or 172.17.0.1 (Linux)
# For local: use localhost
DB_HOST=172.17.0.1
DB_PORT=3306
DB_USER=mcp_user
DB_PASSWORD=Mcp@2026ReadOnly
DB_NAME=mcp_db
DB_CHARSET=utf8mb4
